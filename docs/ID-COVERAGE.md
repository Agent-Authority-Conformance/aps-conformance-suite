# draft-pidlisnyi-aps-03 requirement coverage inventory

This table records which normative requirements of the published Internet-Draft the fixtures and runners in this repository exercise; it makes no claim that a passing run validates the draft.

Draft: `draft-pidlisnyi-aps-03-PUBLISHED.txt`, sha256 `59b9547a20c6d514eceb97d169b92b2f9c70a231f5b914e8fc1b478c7f359fca`.
Suite: commit `b46568e8947291e553c6392403226e8d05be31d2`.

Requirement extraction: every sentence in the draft body carrying an RFC 2119 keyword (MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD NOT, RECOMMENDED, NOT RECOMMENDED, MAY, OPTIONAL), excluding the definition in Section 1.1. Sections 1, 1.1, 3.1, 3.4, 6, 8, 11, 12, 13, 14, 7.2 and Appendix A carry no such sentence and so contribute no rows.

Status definitions. EXERCISED: a named fixture vector plus a runner assertion that fails when the requirement is violated. PARTIAL: related fixture material exists but no assertion that would fail on violation, or only part of the requirement is reached. NOT EXERCISED: neither.

## Requirements

| REQ id | Section | Draft line | Keywords | Status | Fixture | Vector | Runner assertion | Runner command | Requirement | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `REQ-2.1-1` | 2.1 | 232 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A verifier MUST report proof of possession separately from key authority. | No runner emits a proof-of-possession result distinct from key authority. Every receipt runner returns one verdict per signature (valid/invalid), with no separate possession axis. |
| `REQ-2.2-1` | 2.2 | 242 | SHOULD | **PARTIAL** | fixtures/cross-stack/receipts-amdal/receipts-amdal-2026-07-29.json | receipt-2026-07-29-did-uri-invalid | runners/receipt_runner.py:50-53 | `npm run cross-stack:receipts-amdal:verify` | New self-certifying passports SHOULD use an existing generative DID method whose identifier commits to the signing key, such as did:key [DID-KEY]. | A non-did:key:z URI is rejected with DID_URI_INVALID, so the generative form is required where it is used. But the SHOULD is about what a new passport chooses, and the APS-native families use non-generative did:aps: identifiers throughout (fixtures/cross-stack/oracle-safety-check/keys.ts:20); no vector fails for choosing one. Lower status taken. |
| `REQ-2.2-2` | 2.2 | 247 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Deployments that require one stable agent identifier across key rotation MUST use an update-capable DID method and resolve the verification method at the artifact's signing time, for example by a method-defined versionId or versionTime. | No fixture carries a versioned DID document. "versionId" and "versionTime" appear nowhere in the tree; nothing resolves a verification method at an artifact signing time. |
| `REQ-2.2-3` | 2.2 | 256 | MAY | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A compatibility verifier MAY read that exact immutable form. | No raw-hex alias fixture exists; "raw-hex"/"raw_hex" appear nowhere in the tree. |
| `REQ-2.2-4` | 2.2 | 257 | MUST | **PARTIAL** | fixtures/cross-stack/receipts-amdal/receipts-amdal-2026-07-29.json | receipt-2026-07-29-wrong-signer-did, receipt-2026-07-29-did-uri-invalid | runners/receipt_runner.py:50-53 | `npm run cross-stack:receipts-amdal:verify` | It MUST treat the embedded key as the complete identifier state, MUST reject the earlier raw-hex alias, and MUST treat rotation as a new identifier. | The first clause is reached: the key is recovered from the identifier itself (base58btc + multicodec 0xed01 + 32 bytes) and a DID whose embedded key is not the signing key is rejected. The raw-hex-alias clause and the rotation-is-a-new-identifier clause have no vector. Additional DID_URI_INVALID vectors exist in fixtures/cross-stack/receipts-aeoess/receipts-amdal-v2-2026-08-19.json and -2026-08-26.json, but no documented entrypoint runs those files (see the entrypoint inventory). |
| `REQ-2.3-1` | 2.3 | 296 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A verifier MUST NOT infer a higher level from the presence of a field. | No principal-binding-level vectors. Nothing in the tree carries a binding level, so no verifier can be observed inferring one. |
| `REQ-2.4-1` | 2.4 | 311 | MAY | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An update-capable identifier MAY rotate its signing key. | No rotation vector in any family. |
| `REQ-2.4-2` | 2.4 | 313 | MUST | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/*.json (13 vectors) | all | fixtures/cross-stack/oracle-safety-check/verify.ts:339, node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:126 | `npm run verify:oracle-safety-check` | A resolver MUST select the key version authorized at the artifact's issued_at; selecting the key that is current at verification time is insufficient. | The interface carries issuance time: verifyReceiptV1 passes receipt.issued_at to the key resolver. Every fixture resolver discards it - fixtures/cross-stack/oracle-safety-check/verify.ts:339 resolves with `(_s, _k, _i) => resolveKey(receipt.issuer)` - and no vector fails when the verification-time key is selected instead. Related material, no assertion that would fail on violation. |
| `REQ-2.4-3` | 2.4 | 318 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | When key retirement makes the result depend on whether an artifact was signed before a boundary, a profile MUST identify an acceptable timestamp, transparency-log, or equivalent evidence source. | No key-retirement boundary in any fixture, and no timestamp or transparency-log evidence source is named anywhere in the corpus. |
| `REQ-2.5-1` | 2.5 | 341 | MAY | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An external signer identifier MAY be hash-bound to its subject: the identifier carries a digest of the subject identity (for example, a DID whose method-specific identifier is the SHA-256 of a server identity), and the resolver checks the presented subject against that digest before any key material is trusted. | No hash-bound signer identifier anywhere; "hash-bound"/"hash_bound" have zero hits. |
| `REQ-2.5-2` | 2.5 | 350 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A hash-bound locator presented without the subject it binds MUST be rejected. | Follows REQ-2.5-1: no hash-bound locator exists to present without its subject. |
| `REQ-2.5-3` | 2.5 | 353 | MAY | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Published key material MAY carry per-key validity windows. | Published key material in this corpus carries no validity windows. runners/aat_runner.py:141-154 resolves keys from an issuer JWKS by kid; the windows evaluated at runners/aat_runner.py:157-172 are the token's nbf/exp, not per-key windows. |
| `REQ-2.5-4` | 2.5 | 353 | MUST NOT | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/*.json (13 vectors) | all | fixtures/cross-stack/oracle-safety-check/verify.ts:339, node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:126 | `npm run verify:oracle-safety-check` | Key selection is gated on the artifact's own signed issuance time, evaluated against each key's window; a verifier MUST NOT select whichever key is current at verification time. | Same evidence as REQ-2.4-2, and the same gap: the resolver signature accepts issued_at, every fixture resolver ignores it, and no vector distinguishes issuance-time from verification-time key selection. |
| `REQ-2.5-5` | 2.5 | 364 | MAY, MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A deployment MAY adopt a fail-open policy for transport unreachability only; malformed key material that loads but is wrong is not a transient transport condition and MUST fail closed even under such a policy. | No fail-open policy anywhere in the tree ("fail-open"/"fail_open" have zero hits), and no vector presents malformed-but-loadable key material. |
| `REQ-2.5-6` | 2.5 | 367 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A resolution outcome degraded by a fail-open policy MUST NOT be treated as a positive verification, consistent with Section 5.5. | Follows REQ-2.5-5: no degraded resolution outcome exists to be mislabelled. |
| `REQ-2.6-1` | 2.6 | 398 | MUST NOT | **PARTIAL** | fixtures/instruction-provenance/canonicalize-fixture-v1.json | N7 (attestation_tier tampered to "witnessed") | fixtures/instruction-provenance/generate-fixtures.ts:212-252, asserted via runners/ts/verify.ts:238-260 | `npm run verify` | A signal's provenance tier and its verification status are independent axes: a provider-attested signal whose signature was never checked remains at declared, and a verifier MUST NOT infer verification from tier. | Provenance-tier material exists and a tier mutation is rejected, but under InstructionProvenanceReceipt v0.2's own tier lock, which is a different rule from the draft's "tier and verification status are independent axes". fixtures/read-fidelity-receipt/lib.ts:602 validates verification_method as an enum only. Nothing asserts that an unchecked provider-attested signal stays at "declared". |
| `REQ-3.2-1` | 3.2 | 512 | MUST | **NOT EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass | - | `-` | Profiles on a facet MUST match; a profile change is unsupported rather than narrower. | The facet profiles are present in the data (authority.scope.profile "aps-hierarchical-v1", reputation "aps-score-0-100-v1", values "aps-values-identifiers-v1", reversibility "aps-tci-v1") and the SDK carries UNSUPPORTED_PROFILE, but no flip in the declared mutation set (fixtures/cross-stack/oracle-safety-check/verify.ts:601-623) changes a facet profile and no vector declares that outcome. Data presence without an assertion is not coverage. |
| `REQ-3.2-2` | 3.2 | 519 | MUST | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: scope widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:601-604 | `npm run cross-stack:oracle-safety-check:falsify` | Every child grant MUST be covered by a parent grant. | Also reached by interop/aae-envelope/V2-widened-scope-reject.json asserted at interop/aae-envelope/verify.ts:155,200-201, and by fixtures/composition/a2a-1496-negative-paths/01-scope-expansion.fixture.json (INVALID_CLAIM_SCOPE) asserted at fixtures/composition/a2a-1496-negative-paths/verify.ts. |
| `REQ-3.2-3` | 3.2 | 524 | MUST NOT | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: spend widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:605-608 | `npm run cross-stack:oracle-safety-check:falsify` | Amounts are canonical unsigned decimal integers from 0 through 9223372036854775807; per_action MUST NOT exceed cumulative. | The parent-coverage half is asserted: the flip raises per_action and cumulative together and must produce SPEND_WIDENING. The per_action-MUST-NOT-exceed-cumulative clause has no vector - the flip keeps them equal - and neither the canonical unsigned-decimal grammar nor the 9223372036854775807 bound is exercised anywhere. The SDK code PER_ACTION_EXCEEDED is never reached by the corpus. |
| `REQ-3.2-4` | 3.2 | 532 | MUST | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: depth widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:609-611 | `npm run cross-stack:oracle-safety-check:falsify` | Otherwise child.remaining MUST be no greater than parent.remaining minus one. | Also fixtures/composition/a2a-1496-negative-paths/02-depth-violation.fixture.json (DELEGATION_DEPTH_EXCEEDED) at fixtures/composition/a2a-1496-negative-paths/verify.ts. |
| `REQ-3.2-5` | 3.2 | 535 | MUST | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: time widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:612-614 | `npm run cross-stack:oracle-safety-check:falsify` | Time The half-open child interval [not_before, not_after) MUST be contained in the parent interval. | The flip moves the child not_after past the parent's and must produce TIME_WIDENING. |
| `REQ-3.2-6` | 3.2 | 536 | MUST, MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A child's not_before MUST NOT predate its issued_at, and a child MUST be issued while the parent is valid. | The SDK carries ISSUED_AT_OUTSIDE_PARENT, but no flip and no vector produces it. Neither the not_before-predates-issued_at clause nor the issued-while-parent-valid clause has a fixture. |
| `REQ-3.2-7` | 3.2 | 541 | MUST NOT | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: reputation widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:615-617 | `npm run cross-stack:oracle-safety-check:falsify` | A child ceiling MUST NOT exceed its parent. | Raises the parent ceiling to 50 above the child and must produce REPUTATION_WIDENING. |
| `REQ-3.2-8` | 3.2 | 548 | MAY, MUST | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: values weakening) | fixtures/cross-stack/oracle-safety-check/verify.ts:618-620 | `npm run cross-stack:oracle-safety-check:falsify` | A child MUST contain every parent-required identifier and MAY add identifiers. | Emptying the child's values.required must produce VALUES_WEAKENING. Only the removal direction is asserted; the MAY-add direction has no vector. |
| `REQ-3.2-9` | 3.2 | 565 | MUST NOT | **EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass (flip: reversibility widening) | fixtures/cross-stack/oracle-safety-check/verify.ts:621-623 | `npm run cross-stack:oracle-safety-check:falsify` | A child ceiling MUST NOT move to the right. | Moving the child ceiling from "compensable" to "irreversible" must produce REVERSIBILITY_WIDENING. |
| `REQ-3.3-1` | 3.3 | 591 | MUST NOT | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/*.json (13 vectors) | all | fixtures/cross-stack/oracle-safety-check/verify.ts:362-385 | `npm run verify:oracle-safety-check` | A caller MUST NOT collapse indeterminate or unsupported into valid. | The SDK result carries state 'valid'\|'invalid'\|'indeterminate'\|'unsupported' and the runner reads chain.state, but every vector's expected chain outcome is valid, EXPIRED or REVOKED (fixtures/cross-stack/oracle-safety-check/verify.ts:363-370). No vector produces indeterminate or unsupported, so no assertion would fail if a caller collapsed them. |
| `REQ-3.3-2` | 3.3 | 594 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A verifier MUST NOT union scopes or budgets from multiple chains. | Every fixture carries exactly one delegation chain, so multi-chain union has no surface. Nearest adjacent material is the claim-locality property P2 of runners/ts/token-exchange-attenuation.test.ts:8-9, which asserts that evaluating an exchanged token reads only that token's own claims - a different object (RFC 8693 tokens, not APS chains). Lower status taken. |
| `REQ-3.5-1` | 3.5 | 635 | MAY | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/delegation-revoked.json | delegation-revoked | fixtures/cross-stack/oracle-safety-check/verify.ts:362-373 | `npm run verify:oracle-safety-check` | Any delegation MAY be revoked by its issuer. | Revocation is observed and the chain must fail with REVOKED. But revocation enters as a resolver verdict (RevocationResolution active\|revoked\|unknown), not as an act attributable to the delegation's issuer, so the "by its issuer" clause is unasserted. |
| `REQ-3.5-2` | 3.5 | 635 | MUST | **PARTIAL** | interop/aae-envelope/V4-revoked-parent-cascade-reject.json | V4 | interop/aae-envelope/verify.ts:170,200-201 | `npm run verify:aae-envelope` | Revocation MUST initiate a cascade to all transitive descendants; a cascade is complete only when the cascade-completion record of Section 3.5.1 has been emitted. | The cascade half is asserted: a revoked parent invalidates the child subtree in the same verification pass. The completion half is not - the cascade-completion record of Section 3.5.1 does not exist as a fixture, a field or a runner concept anywhere in the tree, so "complete only when the cascade-completion record has been emitted" is unreachable. |
| `REQ-3.5-3` | 3.5 | 639 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | The enforcement gateway MUST recheck revocation status at execution time, not only at approval time. | Stated in terms by the suite itself at fixtures/receipt-decision-relation/verify.ts:29-32: "It also does not exercise dispatch-time enforcement. Consumption of receipt_id, single-use, revocation or time recheck at dispatch, and spend reservation are enforcement-boundary obligations and are not tested here." |
| `REQ-3.5.1-1` | 3.5.1 | 648 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | The model separates two questions that MUST NOT collapse into one mutable lookup: whether a delegation is currently valid, answerable from state, and whether and why a revocation occurred, verifiable from signed records. | No signed revocation record exists in the corpus. Revocation is state (a resolver verdict), which is the mutable-lookup side the requirement separates from; the signed-record side has no artifact. |
| `REQ-3.5.1-2` | 3.5.1 | 653 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A revocation MUST produce a signed revocation record carrying, at minimum: the revoked delegation's identity; the revocation time, inside the signed content; a reference to the revoking authority; and a machine-readable reason code, with optional free-text detail. | No revocation record with a revoked-delegation identity, a signed revocation time, a revoking-authority reference or a machine-readable reason code exists in any fixture. Appendix A bears on any future fixture here: "the TypeScript emitters for Revocation-Observation and Evidence-Bundle records compute signatures and member digests over the project's legacy canonicalization rather than the RFC 8785 profile specified in this document, and verifiers of those records validate over the same form." |
| `REQ-3.5.1-3` | 3.5.1 | 656 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A revocation derived from a cascade MUST additionally carry a reference to the originating revocation, so that a verifier can reconstruct from the records why a descendant delegation died. | Follows REQ-3.5.1-2: no cascade-derived revocation record exists to carry an originating reference. |
| `REQ-3.5.1-4` | 3.5.1 | 667 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Its presence lets a verifier distinguish a fully closed subtree from a revocation in progress from the records alone; its absence means in progress, and a verifier MUST NOT read an incomplete record set as a closed subtree. | Follows REQ-3.5.1-2: no record set exists over which completeness could be read. |
| `REQ-3.6-1` | 3.6 | 696 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An issuer minting a child delegation MUST verify the parent delegation's signature and temporal validity before signing the child, and MUST refuse to issue under an expired, not-yet-valid, or revoked parent. | Every fixture is an issuance-complete artifact. Nothing in the tree runs an issuer, so no issuance can be observed refusing under an expired, not-yet-valid or revoked parent. |
| `REQ-3.6-2` | 3.6 | 699 | MUST, MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Issuance from an expired parent MUST fail at the issuer; it MUST NOT produce a delegation whose invalidity is left for a later verifier to discover. | Same absence. interop/aae-envelope/V3-expired-parent-reject.json demonstrates exactly the later-verifier detection this requirement says is insufficient, which is evidence about the verifier, not about the issuer. |
| `REQ-4-1` | 4 | 720 | MUST, MUST NOT | **NOT EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass | - | `-` | When the action's declared unit and the governing delegation's declared unit differ, the gate MUST deny; it MUST NOT compare raw numeric amounts across units, and it MUST NOT apply a conversion. | Every delegation vector declares spend.unit ("iso4217:USD:minor"), and the SDK carries SPEND_UNIT_CHANGE and UNIT_MISMATCH, but no flip changes a unit and no vector presents an action and a delegation with differing units. The deny, the no-raw-comparison and the no-conversion clauses are all unasserted. |
| `REQ-4-2` | 4 | 734 | MAY, SHOULD | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | pass | - | `npm run verify:oracle-safety-check` | Deployments handling monetary spend SHOULD require actions to declare their unit, and a profile MAY require the gate to deny when it is undeclared. | The declared-unit case is present as data in every delegation vector. No vector omits the unit, and no assertion fails when it is absent, so neither the SHOULD nor the profile MAY is reached. |
| `REQ-4-3` | 4 | 742 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Before dispatch, the boundary MUST atomically reserve the proposed amount against every bounded ancestor in the selected root-to-leaf chain. | No spend reservation anywhere. The SDK exposes BudgetOperationResult with RESERVED/PER_ACTION_EXCEEDED/CUMULATIVE_EXCEEDED, which no fixture reaches; fixtures/receipt-decision-relation/verify.ts:29-32 records spend reservation as out of scope. |
| `REQ-4-4` | 4 | 744 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | It MUST dispatch only if the reservation succeeds for all of them. | Follows REQ-4-3: no reservation exists whose success could gate a dispatch. |
| `REQ-4.1-1` | 4.1 | 789 | MUST | **NOT EXERCISED** | fixtures/actionref-canonical/actionref-canonical-fixture-v1.json | - (shape mismatch) | - | `-` | profile MUST equal "aps-action-ref-v2". agent_id is the acting agent's identifier. action_type is the operation identifier. target is the exact resource, tool, or endpoint against which the action will be dispatched; a profile MUST define its target string construction. | The literal "aps-action-ref-v2" occurs nowhere in the tree. The actionref-canonical inputs are {agentId, actionType, scopeRequired, timestamp}: no profile member, no target, no payload_ref, no nonce. The only "action-ref-v2" in the repository is Argentum's own protocol version under fixtures/cross-stack/argentum-action-ref-v1v2/, a different construction. Neither the profile clause nor the target-construction clause has a fixture. |
| `REQ-4.1-2` | 4.1 | 798 | MAY, MUST | **NOT EXERCISED** | fixtures/actionref-canonical/actionref-canonical-fixture-v1.json | - (no vector) | - | `-` | All string fields MUST be non-empty except that a profile MAY permit an empty scope_required array. | The six vectors are plain-single-scope-ascii, unsorted-multi-scope-ascii, nfd-scope-normalizes-to-nfc, astral-scope-orders-after-bmp-high and the two duplicate negatives. None carries an empty string field and none carries an empty scope_required array. |
| `REQ-4.1-3` | 4.1 | 813 | MUST | **PARTIAL** | fixtures/actionref-canonical/actionref-canonical-fixture-v1.json | negative-duplicate-scope-raw, negative-duplicate-scope-nfc-collision | runners/ts/verify.ts:459-475 (throw), asserted at runners/ts/verify.ts:496-513 | `npm run verify` | A verifier MUST reject an object with an unknown or duplicate member, a non-I-JSON value, a non-canonical scope array, an invalid timestamp, or an invalid hexadecimal field. | One clause of five is reached: a non-canonical (duplicate, including NFC-collision) scope array is rejected and never deduplicated. Invalid timestamps are exercised only for the Section 4.2 legacy form (see REQ-4.2-1), not for the Section 4.1 object. Unknown member, duplicate member, non-I-JSON value and invalid hexadecimal field have no vector. |
| `REQ-4.1-4` | 4.1 | 815 | MUST, MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A parser MUST detect duplicate names before conversion to an ordinary map; it MUST NOT accept the last occurrence silently. | Nothing in the tree parses serialized JSON for duplicate member names. The SDK entry point that could - verifyReceiptV1Serialized - is never called anywhere in fixtures, runners, tests or interop. |
| `REQ-4.1-5` | 4.1 | 819 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A string containing an unpaired UTF-16 surrogate has no UTF-8 encoding and MUST be rejected. | No unpaired-surrogate vector. The astral vector "scope:\U00010400" carries a well-formed pair. The Argentum domain negatives (av-003) reject valid surrogate pairs under that profile's ASCII-only domain rule, which is a different rule from the draft's unpaired-surrogate rejection. |
| `REQ-4.1-6` | 4.1 | 820 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An implementation MUST NOT substitute U+FFFD, delete the code unit, or otherwise repair the string. | Follows REQ-4.1-5: with no unpaired-surrogate input, no vector can observe a U+FFFD substitution, a deletion or any other repair. |
| `REQ-4.1-7` | 4.1 | 826 | MUST | **PARTIAL** | fixtures/cross-stack/action-ref-v1-negatives/vectors.json | recompute-negative vectors | fixtures/cross-stack/action-ref-v1-negatives/run.mjs (header lines 4-7; two independent recomputation paths) | `npm run cross-stack:action-ref-v1-negatives:verify` | The enforcement boundary MUST recompute payload_ref from the payload it will dispatch and action_ref from the received input object. | Recompute-and-fail-closed is asserted, but over action-ref-v1, the Section 4.2 legacy correlation form, not the Section 4.1 object. payload_ref has no fixture at all: the Section 4.1 preimage members target, payload_ref and nonce are absent from every vector in the corpus. |
| `REQ-4.1-8` | 4.1 | 827 | MUST | **PARTIAL** | fixtures/cross-stack/action-ref-v1-negatives/vectors.json | recompute-negative vectors | fixtures/cross-stack/action-ref-v1-negatives/run.mjs (header lines 5-7: "MUST fail closed, before invocation") | `npm run cross-stack:action-ref-v1-negatives:verify` | It MUST reject either mismatch before policy evaluation. | Same evidence and same limit as REQ-4.1-7: the ordering claim is asserted for the v1 form only. |
| `REQ-4.1-9` | 4.1 | 828 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | It MUST also reject reuse of nonce by the same agent within the deployment's replay window. | No Section 4.1 nonce member exists in any vector, so no reuse can be presented. The "nonce" hits in the tree are AuthorityDelegationV1's own nonce and RFC 9421's. |
| `REQ-4.2-1` | 4.2 | 869 | MUST, MUST NOT | **EXERCISED** | fixtures/cross-stack/argentum-action-ref-v1v2/action-ref-v1-domain-negative/action-ref-v1-domain-negative.fixture.json | adn-001, adn-002, adn-003, adn-004, adn-005 | fixtures/cross-stack/argentum-action-ref-v1v2/action-ref-v1-domain-negative/validate.py:63-65 | `npm run cross-stack:argentum-action-ref-v1v2:verify` | An implementation MUST reject a timestamp that does not match this shape; it MUST NOT coerce, truncate, extend, or renormalize a non-conforming value. | Five malformed timestamp shapes (lowercase t, lowercase z, +00:00 offset, second precision, six fractional digits), each of which must be rejected; the validator additionally asserts that zero SHA-256 digests are computed for a rejected vector (fixtures/cross-stack/argentum-action-ref-v1v2/action-ref-v1-domain-negative/validate.py:15-17), which is the no-coerce/no-renormalize clause. Also reached by the grammar gate in fixtures/cross-stack/action-ref-v1-negatives/run.mjs. |
| `REQ-4.2-2` | 4.2 | 879 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | It omits the target, payload digest, and nonce, and therefore MUST NOT identify an APS policy decision, approval, dispatch, spend reservation, or receipt. | No vector presents a v1 digest in an APS policy-decision, approval, dispatch, spend-reservation or receipt slot and requires it to be refused. |
| `REQ-4.2-3` | 4.2 | 881 | MUST, MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An implementation MUST label it "action-ref-v1-jcs-sha256" and MUST NOT present its digest as an action_ref under Section 4.1. | The literal "action-ref-v1-jcs-sha256" occurs once in the tree, as prose in fixtures/cross-stack/nobulex-bilateral-v0/MAPPING.md:8. No vector carries the label and no runner asserts either the labelling clause or the do-not-present-as-action_ref clause. |
| `REQ-4.3-1` | 4.3 | 914 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | At consumption the boundary MUST re-validate what can have changed since issuance, including revocation status and temporal validity, as required by Section 3.5. | Stated as untested by the suite at fixtures/receipt-decision-relation/verify.ts:29-32. Nearest adjacent material is interop/mih-sato-composition-00/corpus/examples/composition/caid-aec-aeb-capsule-v1/run.mjs, whose reason codes include already_consumed and timeout_before_dispatch; its README documents only the two adapters (adapter/ts/run.ts, adapter/py/run.py) and no documented command runs that file. |
| `REQ-4.3-2` | 4.3 | 916 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An approval that has expired, has already been consumed, or fails re-validation MUST NOT admit execution. | Same absence and same adjacent material as REQ-4.3-1. |
| `REQ-5.1-1` | 5.1 | 979 | MUST, REQUIRED | **PARTIAL** | fixtures/cross-stack/receipts-aeoess/receipts-aeoess-2026-08-27.json | aps-receipt-2026-08-27-issuer-plus-cosigner-a, -b | runners/aeoess_receipt_runner.py:91-92 (profile), node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:49-50 | `npm run cross-stack:receipts-aeoess:verify and npm run verify:oracle-safety-check` | profile MUST equal "aps-receipt-v1". receipt_type identifies a stage defined in Section 5.3. issuer is the party responsible for that record. subject_agent is the acting agent. action_ref is the aps- action-ref-v2 digest from Section 4.1. delegation_ref identifies the selected AuthorityDelegationV1 leaf, or the authority basis when no delegation exists. decision_ref is REQUIRED for policy-decision and action-result records and MUST be absent from an action-intent record. | The profile clause is asserted in two independent implementations. The type-conditional clause is not: neither validator conditions on receipt_type, and decision_ref is only range-checked when present (runners/aeoess_receipt_runner.py:99-100, node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:57-58). The intent vectors happen to omit decision_ref, but no vector adds one to an action-intent record and nothing fails if one is added. |
| `REQ-5.1-2` | 5.1 | 986 | MUST, REQUIRED | **PARTIAL** | fixtures/cross-stack/receipts-aeoess/receipts-aeoess-2026-08-27.json; fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/pass.json | aps-receipt-2026-08-27-issuer-plus-cosigner-a; pass | runners/aeoess_receipt_runner.py:103-107 (issued_at), fixtures/cross-stack/oracle-safety-check/verify.ts:351-353 (decision.prev == intent.receipt_id) | `npm run cross-stack:receipts-aeoess:verify and npm run verify:oracle-safety-check` | issued_at uses exact UTC milliseconds. prev is REQUIRED for policy-decision and action-result records and MUST be absent from an action-intent record. | Exact UTC milliseconds is asserted, and prev-present-and-correct is asserted for a policy-decision record. The MUST-be-absent-from-an-action-intent clause is not conditioned on receipt_type in either validator (runners/aeoess_receipt_runner.py:101-102, node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:59-60). |
| `REQ-5.1-3` | 5.1 | 997 | MUST | **EXERCISED** | fixtures/cross-stack/receipts-aeoess/receipts-aeoess-2026-08-27.json | aps-receipt-2026-08-27-issuer-plus-cosigner-a, -b (two signatures each) | runners/aeoess_receipt_runner.py:128-144; node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:78-92 | `npm run cross-stack:receipts-aeoess:verify` | Each signer and key_id pair occurs at most once, alg MUST equal "Ed25519", and one signature MUST be from issuer. | All three clauses are asserted - duplicate (signer, key_id) pair, alg != "Ed25519", and a missing issuer signature each produce a validation error, which makes the vector's result "invalid" against its declared expected_result "valid" and exits nonzero. No declared negative vector carries these mutations; the assertion is reached over the positive vectors and fails when they are violated. |
| `REQ-5.2-1` | 5.2 | 1036 | MUST | **PARTIAL** | fixtures/cross-stack/receipts-aeoess/receipts-aeoess-2026-08-27.json | aps-receipt-2026-08-27-issuer-plus-cosigner-a, -b | runners/aeoess_receipt_runner.py:158-190 (receipt_id recomputed at :167 before signature verification at :171-186) | `npm run cross-stack:receipts-aeoess:verify` | A verifier MUST recompute receipt_id before signature verification, resolve each key as authorized for signer at issued_at, and verify every required signature. | The recompute-before-verify ordering and the verify-every-signature clause are asserted. The "resolve each key as authorized for signer at issued_at" clause is not: the key table is keyed by (signer, key_id) with no time dimension (runners/aeoess_receipt_runner.py:192), and the OSC resolver discards both arguments (fixtures/cross-stack/oracle-safety-check/verify.ts:339). |
| `REQ-5.3.1-1` | 5.3.1 | 1052 | MUST | **PARTIAL** | fixtures/cross-stack/receipts-aeoess/receipts-aeoess-2026-08-27.json | aps-receipt-2026-08-27-issuer-plus-cosigner-a | - (no receipt_type-conditional assertion) | `npm run cross-stack:receipts-aeoess:verify` | An action-intent record has receipt_type "aps:action-intent:v1". issuer and subject_agent MUST be the acting agent, prev and decision_ref MUST be absent, and result MUST contain exactly profile "aps-action-intent-result-v1" and status "declared". | The vector carries the requirement's exact form: receipt_type "aps:action-intent:v1", issuer == subject_agent, no prev, no decision_ref, and result exactly {profile: "aps-action-intent-result-v1", status: "declared"}. No runner asserts any of it - runners/aeoess_receipt_runner.py:80-145 never reads receipt_type, so all four clauses would survive mutation. The fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/intent receipts carry the same receipt_type with a free-form result and would also survive. |
| `REQ-5.3.2-1` | 5.3.2 | 1094 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Before dispatch, the enforcement boundary MUST verify that it has not expired, atomically consume its receipt_id, recheck time and revocation state, and complete any spend reservation. | Stated as untested at fixtures/receipt-decision-relation/verify.ts:29-32; no consumption, single-use, recheck or reservation surface exists. |
| `REQ-5.3.2-2` | 5.3.2 | 1097 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An already consumed, expired, or stale approval MUST NOT admit dispatch. | Follows REQ-5.3.2-1: no vector presents an already-consumed, expired or stale approval at a dispatch point. |
| `REQ-5.3.2-3` | 5.3.2 | 1098 | MUST NOT | **NOT EXERCISED** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/block.json, fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/authority-denied.json | - (no consumption attempt) | - | `-` | A deny record is terminal and MUST NOT be consumed as an approval. | Deny outcomes exist as vectors, but nothing attempts to consume one as an approval, so the terminality clause is unasserted. |
| `REQ-5.3.3-1` | 5.3.3 | 1103 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | An action-result record has receipt_type "aps:action-result:v1". issuer is the enforcement boundary, prev is the consumed policy- decision receipt_id, and decision_ref MUST equal that decision's decision_ref. result contains exactly profile, status, effect_ref, and error_code: | No aps:action-result:v1 receipt exists anywhere in the tree; the literal has zero hits. Neither the issuer clause, the prev clause nor the decision_ref-equality clause has an artifact. |
| `REQ-5.3.3-2` | 5.3.3 | 1125 | REQUIRED | **NOT EXERCISED** | n/a | n/a | n/a | n/a | For succeeded, effect_ref is REQUIRED and error_code is null. | The literal "effect_ref" has zero hits in the tree. |
| `REQ-5.4-1` | 5.4 | 1170 | MUST | **PARTIAL** | fixtures/receipt-decision-relation/receipt-decision-relation-v1/pass.json | pass, substitution, temporal-earlier, temporal-equal (+ 3 flipped) | fixtures/receipt-decision-relation/verify.ts:175 (decision_ref recomputed independently) | `npm run verify:receipt-decision-relation` | authority_state MUST include the selected authority chain, authority- basis resolution, revocation observations, and the spend state used by the decision. policy_input MUST identify the policy and version actually evaluated and include its decision-relevant input. decision_context contains decision-relevant environmental state. decision_output is the exact CoreDecisionOutputV1 carried in the policy-decision receipt. | The four-component DecisionRefV1 preimage is recomputed from the domain-separated construction without importing the SDK, and a mismatch is DECISION_REF_MISMATCH. policy_input carries policy_id and policy_version, so that clause is reached in the data. authority_state in every vector is {profile, delegation_ref, subject_agent, scope}: no selected authority chain, no authority-basis resolution, no revocation observations and no spend state, and nothing asserts their presence. Appendix A on this section: the released implementations "do not publish a decision_ref computation carrying all properties described in Section 5.4". |
| `REQ-5.4-2` | 5.4 | 1184 | MAY | **NOT EXERCISED** | fixtures/receipt-decision-relation/receipt-decision-relation-v1/*.json | - (no vector) | - | `-` | A deployment MAY keep component values private, but a verifier cannot independently recompute a component whose value is unavailable. | Every vector supplies all four components in full; no vector withholds one, so the private-component case is never reached. |
| `REQ-5.5-1` | 5.5 | 1203 | MUST NOT | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/evidence-missing.json | evidence-missing | fixtures/cross-stack/oracle-safety-check/verify.ts:388-395 (evidence_ref_absent / evidence_digest_mismatch into the derived reason set) | `npm run verify:oracle-safety-check` | A verifier MUST NOT report the containing receipt fully valid for a claim that depends on unresolved evidence. | Unresolved evidence is detected and forced into the composite gate's reason set, which the runner asserts equals the vector's expectReasons exactly. But the receipt-level verdict is not withheld: verifyReceiptV1 still reports the containing decision receipt valid, and the gate verdict is a separate object. The requirement is about the receipt's own validity report. |
| `REQ-5.5-2` | 5.5 | 1206 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Retrieval code MUST apply scheme allowlists, size limits, redirect limits, timeouts, and private-network restrictions appropriate to its environment. | No retrieval code exists in the tree. Every evidence reference resolves in-document, so scheme allowlists, size limits, redirect limits, timeouts and private-network restrictions have no surface. |
| `REQ-5.6-1` | 5.6 | 1227 | MUST NOT | **PARTIAL** | fixtures/cross-stack/oracle-safety-check/oracle-safety-check-v1/*.json (13 vectors) | all | fixtures/cross-stack/oracle-safety-check/verify.ts:362-385 | `npm run verify:oracle-safety-check` | A caller MUST NOT collapse indeterminate or unsupported into valid. | Same evidence and same gap as REQ-3.3-1: the four-state vocabulary exists and is read, but no vector produces indeterminate or unsupported. interop/mih-sato-composition-00 carries a five-value terminal vocabulary with a precedence fold (SCOPE.md) and its adapters run here, but over the composition pack's own artifacts, not APS receipt verification. |
| `REQ-7.1-1` | 7.1 | 1263 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | In an APS-mediated MCP deployment, privileged actions covered by APS policy MUST pass through the gateway, which validates the delegation chain, evaluates the policy chain, and generates signed receipts. | Appendix A: "the enforcement gateway described in Section 7.1 is a separate deployment component and is not part of the published packages." interop/mcp-audit-gateway-v0.6-cleanroom-a0f14a0/recompute.py runs here over 20 canonicalization vectors plus a checkpoint, but recomputes digests only; it asserts nothing about which actions are routed through a gateway. |
| `REQ-7.3-1` | 7.3 | 1313 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Two anchors are kept separate and MUST NOT be merged. | No fixture carries both a source-grant reference and a delegation chain root as separate anchors. fixtures/cross-stack/receipts-aeoess/receipts-amdal-v2-2026-08-26.json discusses delegation_chain_root in prose and runners/aeoess_receipt_runner.py recomputes a chain root where the vector declares one, but no documented entrypoint runs that file and nothing asserts non-merger. |
| `REQ-7.3-2` | 7.3 | 1326 | MUST NOT | **NOT EXERCISED** | n/a | n/a | n/a | n/a | A spend dimension MUST NOT be invented at import: because AuthorityDelegationV1 requires all seven facets, an external grant that carries no spend semantics cannot be projected into a conforming chain root unless an importing profile supplies an explicit spend basis accepted by verifier policy; otherwise the projection is unsupported. | Every AuthorityDelegationV1 vector already carries a spend facet. No import or projection vector exists, so an external grant with no spend semantics is never presented and "unsupported" is never the required outcome. |
| `REQ-7.3-3` | 7.3 | 1331 | MUST | **EXERCISED** | fixtures/cross-stack/token-exchange-attenuation-v0/vectors.json | audience-mismatch / read-at-billing (against_t2=deny, deny_reason=audience_mismatch) | runners/ts/token-exchange-attenuation.test.ts:174-206 | `npm run cross-stack:token-exchange-attenuation-v0:verify` | The grant's audience is carried in the binding but not enforced by it; the relying party at the point of use MUST reject an audience mismatch. | The evaluator denies at the point of use on audience mismatch and the runner asserts the per-request expectation; a request with no declared expectation is itself a failure (runners/ts/token-exchange-attenuation.test.ts:180-186). The object is an RFC 8693 exchanged token, which is the binding this section specifies. |
| `REQ-9-1` | 9 | 1594 | MUST | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Verifiers MUST treat receipts as evidence of what was attested, not as proof of what is true. | A framing requirement about how a verifier reports, with no observable per-vector failing condition anywhere in the corpus. Recorded rather than argued: no fixture distinguishes "attested" from "true". |
| `REQ-9-2` | 9 | 1601 | SHOULD | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Approval signatures over any governance mutation SHOULD bind the full proposed content and the version transition it applies to, not a reference to them. | No governance-mutation artifact exists in any fixture. |
| `REQ-9-3` | 9 | 1608 | MUST | **PARTIAL** | fixtures/cross-stack/aat-amdal/aat-amdal-2026-08-26.json | aat-2026-08-26-expired (exp_reject), aat-2026-08-26-not-yet-valid (nbf_reject) | runners/aat_runner.py:157-172, compared against expected_result at runners/aat_runner.py:355-365 | `npm run cross-stack:aat-amdal:verify` | Where a deployment issues consumable challenge, token, or approval artifacts carrying an expiry (for example, the approval artifact of Section 4.3, or challenge artifacts in a protocol binding), verifiers MUST enforce the expiry, and issuance against an expired or unparseable challenge MUST fail closed. | Expiry enforcement on a token artifact in a protocol binding is asserted, with both bounds kept as distinct outcomes. Also fixtures/composition/a2a-1496-negative-paths/04-validity-expired.fixture.json (VALIDITY_EXPIRED) and interop/aae-envelope/V3-expired-parent-reject.json. The second clause - issuance against an expired or unparseable challenge MUST fail closed - has no issuance path in the tree, so only part of the requirement is reached. |
| `REQ-9-4` | 9 | 1616 | MUST NOT | **PARTIAL** | fixtures/cross-stack/action-ref-v1-negatives/vectors.json | recompute-negative vectors | fixtures/cross-stack/action-ref-v1-negatives/run.mjs | `npm run cross-stack:action-ref-v1-negatives:verify` | A matching external action reference is evidence of correlation between records, not of the authenticity, authority, or integrity of either record, and it MUST NOT be treated as an authority claim; correlation strength is bounded by the external ecosystem's own field discipline. | Mismatch is asserted to fail closed. Nothing asserts the converse the requirement actually states: that a MATCHING external action reference is refused as an authority claim. fixtures/cross-stack/nobulex-bilateral-v0/MAPPING.md:8 names the correlation form in prose only. |
| `REQ-9-5` | 9 | 1643 | SHOULD | **NOT EXERCISED** | n/a | n/a | n/a | n/a | The caller-signed verification attestation exists to make that party identifiable; a verifier that accepts imported roots accepts the importing party as a trust anchor for those chains, and SHOULD treat the attestation as the auditable record of that acceptance. | No caller-signed verification attestation exists in the tree; the artifact the Section 7.3 binding names is absent, so nothing treats it as an auditable record. |
| `REQ-9-6` | 9 | 1653 | MUST, MUST NOT | **PARTIAL** | interop/scitt-cose-vectors-ietf126/ | - (SKIPPED in this run) | interop/scitt-cose-vectors-ietf126/verifier/cbor.ts, verifier/selfattack.ts | `VECTORS_DIR=./scitt-cose/test-vectors OUT_DIR=. npx tsx verifier/run.ts` | Length, count, or size fields carried inside an artifact MUST NOT drive reads or allocation beyond the input actually received: a declared element count that exceeds the remaining input is a decoding error, and a read past the end of the input MUST fail rather than yield default values. | A bounded-decoding verifier and a self-attack harness exist and are exactly this requirement's class, but the documented command needs an external test-vector checkout that is not present, and the run records it SKIPPED. Nothing in the executable set asserts that a declared length or count cannot drive a read past the end of the input. |
| `REQ-9-7` | 9 | 1657 | SHOULD | **PARTIAL** | fixtures/composition/a2a-1496-negative-paths/*.fixture.json | 01-scope-expansion, 02-depth-violation, 03-signature-substitution, 04-validity-expired | fixtures/composition/a2a-1496-negative-paths/verify.ts (asserts the thrown NegativePathError code equals expected_error_code; a non-throw is a failure) | `npm run verify:a2a-1496-negative-paths` | Verification interfaces SHOULD handle a structurally malformed artifact through a defined result, whether a return value or a typed error, rather than an unhandled failure, so that malformed input cannot crash a verifier. | A defined typed result is asserted for four negatives, and node_modules/agent-passport-system/dist/src/v2/receipt-core/receipt.js:121-128 returns {valid:false, errors:[...]} rather than throwing. But all four negatives are semantic (scope, depth, signature, validity), not structurally malformed input, so the requirement's actual subject - a structurally malformed artifact - is not reached. |
| `REQ-10-1` | 10 | 1670 | SHOULD | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Deployments SHOULD disclose only the fields and evidence references needed for the verifier's decision, SHOULD avoid personal data in identifiers and free-text fields, and SHOULD define access and retention policies for passports, delegations, receipts, and evidence records. | Three deployment-policy SHOULDs with no fixture surface. The repository-hygiene workflow checks the repository (absolute paths, secret-shaped files, CRLF), not vector content, and no fixture is a disclosure decision. |
| `REQ-10-2` | 10 | 1676 | SHOULD | **NOT EXERCISED** | n/a | n/a | n/a | n/a | Where global correlation is unnecessary, profiles SHOULD use context-specific agent and principal identifiers. | Every family reuses stable identifiers across its vectors - fixtures/actionref-canonical uses did:aps:zVectorAgent01 in all six, fixtures/cross-stack/oracle-safety-check/keys.ts pins one principal, agent and gateway DID for all thirteen. Nothing exercises context-specific identifiers, and nothing fails if they are not used. |

## Counts

| Status | Requirements |
|---|---|
| EXERCISED | 9 |
| PARTIAL | 24 |
| NOT EXERCISED | 49 |
| **Total** | **82** |

Per section:

| Section | Title | Total | EXERCISED | PARTIAL | NOT EXERCISED |
|---|---|---|---|---|---|
| 2.1 | Agent Passport | 1 | 0 | 0 | 1 |
| 2.2 | Agent Identifiers | 4 | 0 | 2 | 2 |
| 2.3 | Principal Binding | 1 | 0 | 0 | 1 |
| 2.4 | Key Rotation and Historical Verification | 3 | 0 | 1 | 2 |
| 2.5 | Key Resolution for External and Evidence Signers | 6 | 0 | 1 | 5 |
| 2.6 | Attestation Provenance and Composition | 1 | 0 | 1 | 0 |
| 3.2 | Component Orders | 9 | 6 | 1 | 2 |
| 3.3 | Chain Verification | 2 | 0 | 1 | 1 |
| 3.5 | Cascade Revocation | 3 | 0 | 2 | 1 |
| 3.5.1 | Revocation Evidence | 4 | 0 | 0 | 4 |
| 3.6 | Core Invariants | 2 | 0 | 0 | 2 |
| 4 | Policy Chain | 4 | 0 | 1 | 3 |
| 4.1 | Action Reference Computation | 9 | 0 | 3 | 6 |
| 4.2 | Legacy External Correlation Form | 3 | 1 | 0 | 2 |
| 4.3 | Two-Phase Execution | 2 | 0 | 0 | 2 |
| 5.1 | ReceiptV1 Envelope | 3 | 1 | 2 | 0 |
| 5.2 | Receipt Identifiers and Signatures | 1 | 0 | 1 | 0 |
| 5.3.1 | Action Intent | 1 | 0 | 1 | 0 |
| 5.3.2 | Policy Decision | 3 | 0 | 0 | 3 |
| 5.3.3 | Action Result | 2 | 0 | 0 | 2 |
| 5.4 | DecisionRefV1 | 2 | 0 | 1 | 1 |
| 5.5 | Evidence Resolution | 2 | 0 | 1 | 1 |
| 5.6 | Receipt Verification | 1 | 0 | 1 | 0 |
| 7.1 | MCP Binding | 1 | 0 | 0 | 1 |
| 7.3 | OAuth Identity-Assertion Grant Binding | 3 | 1 | 0 | 2 |
| 9 | Security Considerations | 7 | 0 | 4 | 3 |
| 10 | Privacy Considerations | 2 | 0 | 0 | 2 |

## NOT EXERCISED

- `REQ-2.1-1` (Section 2.1, line 232)
- `REQ-2.2-2` (Section 2.2, line 247)
- `REQ-2.2-3` (Section 2.2, line 256)
- `REQ-2.3-1` (Section 2.3, line 296)
- `REQ-2.4-1` (Section 2.4, line 311)
- `REQ-2.4-3` (Section 2.4, line 318)
- `REQ-2.5-1` (Section 2.5, line 341)
- `REQ-2.5-2` (Section 2.5, line 350)
- `REQ-2.5-3` (Section 2.5, line 353)
- `REQ-2.5-5` (Section 2.5, line 364)
- `REQ-2.5-6` (Section 2.5, line 367)
- `REQ-3.2-1` (Section 3.2, line 512)
- `REQ-3.2-6` (Section 3.2, line 536)
- `REQ-3.3-2` (Section 3.3, line 594)
- `REQ-3.5-3` (Section 3.5, line 639)
- `REQ-3.5.1-1` (Section 3.5.1, line 648)
- `REQ-3.5.1-2` (Section 3.5.1, line 653)
- `REQ-3.5.1-3` (Section 3.5.1, line 656)
- `REQ-3.5.1-4` (Section 3.5.1, line 667)
- `REQ-3.6-1` (Section 3.6, line 696)
- `REQ-3.6-2` (Section 3.6, line 699)
- `REQ-4-1` (Section 4, line 720)
- `REQ-4-3` (Section 4, line 742)
- `REQ-4-4` (Section 4, line 744)
- `REQ-4.1-1` (Section 4.1, line 789)
- `REQ-4.1-2` (Section 4.1, line 798)
- `REQ-4.1-4` (Section 4.1, line 815)
- `REQ-4.1-5` (Section 4.1, line 819)
- `REQ-4.1-6` (Section 4.1, line 820)
- `REQ-4.1-9` (Section 4.1, line 828)
- `REQ-4.2-2` (Section 4.2, line 879)
- `REQ-4.2-3` (Section 4.2, line 881)
- `REQ-4.3-1` (Section 4.3, line 914)
- `REQ-4.3-2` (Section 4.3, line 916)
- `REQ-5.3.2-1` (Section 5.3.2, line 1094)
- `REQ-5.3.2-2` (Section 5.3.2, line 1097)
- `REQ-5.3.2-3` (Section 5.3.2, line 1098)
- `REQ-5.3.3-1` (Section 5.3.3, line 1103)
- `REQ-5.3.3-2` (Section 5.3.3, line 1125)
- `REQ-5.4-2` (Section 5.4, line 1184)
- `REQ-5.5-2` (Section 5.5, line 1206)
- `REQ-7.1-1` (Section 7.1, line 1263)
- `REQ-7.3-1` (Section 7.3, line 1313)
- `REQ-7.3-2` (Section 7.3, line 1326)
- `REQ-9-1` (Section 9, line 1594)
- `REQ-9-2` (Section 9, line 1601)
- `REQ-9-5` (Section 9, line 1643)
- `REQ-10-1` (Section 10, line 1670)
- `REQ-10-2` (Section 10, line 1676)

