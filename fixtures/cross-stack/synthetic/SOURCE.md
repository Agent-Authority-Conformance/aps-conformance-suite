# synthetic

This directory contains lab-authored deterministic regression fixtures generated by
runners/generate_synthetic_regression.py with its published seed. It is not an ingestion
from an external system and is not governed by the external-system family admission
rule. Its placement under fixtures/cross-stack/ is historical; the path is retained to
avoid breaking existing references.
